import { test, moduleFor } from 'ember-qunit';

import quakeName from 'arena/helpers/quake-name';

test('quake name should be converted to colored html', function() {
    // console.error(123);

    // equal(quakeName('^1test'), 'test1');
});
