<table class="table">
<tr>
    <th>Online</th>
    <th>Name</th>
    <th>Team</th>
    <th>Score</th>
</tr>
{{#each}}
<tr>
    <td>{{#if online}}<span class="glyphicon glyphicon-ok"></span>{{/if}}</td>
    <td>{{#link-to 'player' this}}{{quake-name name}}{{/link-to}} {{#if skill}}(bot){{/if}}</td>
    <td>{{team}}</td>
    <td>{{score}}</td>
</tr>
{{/each}}
</table>