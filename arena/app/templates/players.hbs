<table class="table">
<tr>
    <th>Online</th>
    <th>Name</th>
    <th>Team</th>
    <th>Score</th>
</tr>
{{#each}}
<tr>
    <td>{{#if online}}<span class="glyphicon glyphicon-ok"></span>{{/if}}</td>
    <td>{{name}}</td>
    <td>{{team}}</td>
    <td>{{score}}</td>
</tr>
{{/each}}
</table>